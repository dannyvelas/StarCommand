# yaml-language-server: $schema=https://taskfile.dev/schema.json
version: "3"

tasks:
  build:labctl:
    internal: false
    env:
      CGO_ENABLED: 1
      CC: clang
      CXX: clang++
    sources:
      - "./**/*.go"
      - "go.mod"
      - "go.sum"
    generates:
      - "./bin/labctl"
    cmds:
      - go build -o ./bin/labctl ./cmd/labctl

  setup:proxmox:check:
    desc: "Check requirements for Proxmox"
    deps: [build:labctl]
    cmds:
      - ./bin/labctl resolve proxmox --requirements
    run: once

  setup:proxmox:
    desc: This task sets up proxmox after booting it for the first time.
    deps: [build:labctl]

    cmds:
      - |
        CONF_JSON=$(./bin/labctl resolve proxmox)
        NODE_IP=$(echo $CONF_JSON | jq -r '.node_ip')
        if ssh -p 17031 -o ConnectTimeout=3 admin@{{.NODE_IP}} exit 2>/dev/null; then
          ansible-playbook -i ansible/inventory.ini ansible/setup-proxmox.yml --ask-vault-pass -e "$CONF_JSON"
        else
          ansible-playbook -i ansible/inventory.ini ansible/setup-proxmox.yml -u root --ask-vault-pass -e "$CONF_JSON"
        fi
